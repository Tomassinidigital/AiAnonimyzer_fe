# Base URL delle API del backend FastAPI.
# Per i test locali punta all'istanza uvicorn (default :8000).
# In produzione: l'URL pubblico del backend (es. https://api.example.com).
VITE_API_BASE_URL=http://localhost:8000
